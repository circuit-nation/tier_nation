name: Ping Server

on:
  workflow_dispatch:
  schedule:
    - cron: '*/12 * * * *' # Every 12 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Check health
        run: curl -fsS https://tier-nation.onrender.com/health
        id: health_check
