{
  "name": "tier_nation",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm dev:client",
    "build": "pnpm build:client",
    "dev:client": "pnpm --filter client dev",
    "build:client": "pnpm --filter client build",
    "lint:client": "pnpm --filter client lint",
    "preview:client": "pnpm --filter client preview",
    "dev:server": "cd server && go run ./cmd/server",
    "build:server": "cd server && go build ./cmd/server"
  }
}
